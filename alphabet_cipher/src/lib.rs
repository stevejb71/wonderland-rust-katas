pub fn encode(keyword: &str, message: &str) -> String {
    panic!("todo")
}

pub fn decode(keyword: &str, message: &str) -> String {
    panic!("todo")
}

pub fn decipher(encoded: &str, original: &str) -> String {
    panic!("todo")
}
