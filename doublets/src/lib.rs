/// Return `None` if there is no possible doublet between `start` and `end`.
/// Otherwise, return a `Vector` containing the `start` and `end` words, and all words in between, in order.
pub fn doublets(start: &str, end: &str) -> Option<Vec<String>> {
	panic!("implement")
}
